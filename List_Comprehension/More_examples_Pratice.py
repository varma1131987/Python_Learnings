# From nums = [1, -4, 3, 0, -2, 5], create a list of only negative numbers using list comprehension.

# From names = ["Anu", "Vikram", "Bob", "Chitra"], create list of names longer than 3 characters.

# From nums = [1, 2, 3, 4, 5], create list of cubes.

# Given sentence = "python list comprehension is powerful", create list of word lengths.

# Given matrix = [[1, 2, 3], [4, 5, 6]], create list of squares of all elements using nested comprehension.

# From nums = [0, 1, 2, 3, 4, 5], create list: "zero" for 0, "even" for other even numbers, "odd" for odd numbers (use if–elif–else style expression).

# Create a list of pairs (x, y) for x in [1,2,3] and y in [4,5] where x + y is even.

# Write loop code for any one comprehension above and ask students to convert it back to a comprehension (reverse direction